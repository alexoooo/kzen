# Project data — one type language, configurable formats, and durable project storage

> **Status: analysis. Nothing here is built.** This is the single consolidated record of the project-data
> design: the *structural data* arc (one recursive type language, generated expression access, formats as
> configured values, provider-neutral content access) and the *durable project storage* capability (a
> general, transactional, queryable store for project data that is not notation). It consolidates and
> replaces four earlier documents — the 2026-08-25 structural-data analysis and its review, the 2026-08-26
> business-state analysis, and the 2026-08-26 project-storage review — all removed on 2026-08-26; their
> corrections are applied in place, not appended.
>
> It is the structural-type arc [`2026-08-20_data-source-model.md`](2026-08-20_data-source-model.md) §4.3
> deferred and master-plan ledger row 14 parked ("declared types retained for future typed-lane work"). It
> supersedes neither that source model, its
> [`Job adapter`](2026-08-20_job-data-source.md), nor
> [`2026-08-21_extension-points.md`](2026-08-21_extension-points.md): `DataRef` / `DataPart` / `DataUnit` /
> `DataManifest`, `DataSource.resolve` and `DataOpener.open` are assumed correct and not revisited, and the
> extension-point decisions are cited where a decision here depends on them. Since 2026-08-27 the type
> language, shape envelope, runtime value and Worker carrier are owned by the
> [unified data model](2026-08-27_data-model.md); §5, §8.1, §9.3 and §11 here point there and record this
> arc's departures, while formats-as-values, providers and content access, structural readers and their
> policies, expression-accessor generation and durable storage remain here. Per CC-20 no
> line numbers are cited; anchors are class / file / doc-section names. Decisions are collected in **§16**
> (`ST*` structural, `PS*` project storage); everything not listed there is a proposal, not a settlement.
> Sessions append to **§18** as they land.

## 1. What this is for

### 1.1 Structural data — six forces

Stated by the user on 2026-08-25, six forces the current model cannot hold at once:

- **Report is a prototype, not the target.** It demonstrated the minimum capabilities; it is not a
  comprehensive solution to emulate. Its value now is the lessons — §2.
- **Nominal and structural types are two worlds with a stringly bridge.** Compiled JVM types resolved on
  the backend, versus schemas carried by the data (a CSV header, a JSON shape). Execution is entirely
  server-side, so there is no client/server reason to keep them apart — only inertia.
- **Dynamic expressions should read both uniformly.** If a structured CSV is accessed by expression, the
  same must hold for a `JobMessage.payload`, and for a nested value inside either.
- **Arbitrary structured data, not just JSON.** Nested JSON is one example. XML, Avro, Parquet, protobuf,
  YAML, fixed-width, EDI, log formats, DB rows. When the schema is known, access must be ergonomic; when
  it is not, there must be a working fallback.
- **Configurable format instances specified in the UI, without writing code.** Delimiters, quoting, record
  selectors, schemas and decoding policy should be configurable for supported format families. Arbitrary
  EDI, binary or log grammars are a separate parser-DSL problem, not a promise of this arc.
- **Selection is distinct from processing.** Files may come from disk or an S3 bucket; the logic that
  processes them is the same either way.

And one constraint, added the same day: **the flat path's allocation-free scalar access and reuse machinery
are properties worth preserving.** The current Job lane is not zero-allocation end to end; §9 makes the
measurable boundaries explicit.

### 1.2 Durable project storage — the question, and its scope

kzen has two kinds of state today: **notation** (YAML documents, the whole graph in memory, CQRS commands
and events, format-preserving deparse) and a smaller tier of **server-owned files** (compiled code, report
runs, Job worker output, all catalogued by `ManagedStorageRegistry`). Many applications built on kzen need a
third kind: **records** — high-volume, transactional, queried by aggregate, retained for years. The question
asked on 2026-08-26 was how such state integrates with kzen and how it distributes cross-platform.

The answer is a **general kzen capability**, and the design keeps four layers distinct:

1. **Generic capability** — durable project data outside notation, with transactions, queries, migrations,
   lifecycle, managed-storage visibility and backup participation (§12).
2. **Default implementation** — an embedded relational store, SQLite as the recommended first engine (§12.5).
3. **Domain-owned usage** — a ledger, CRM, inventory system, case-management application, scheduler or any
   other consumer supplies its own schema, repositories, rules and UI.
4. **Motivating example** — a small-business ledger / tax application demonstrates demanding requirements
   (§14) but contributes no framework-wide business semantics.

The general ledger, inventory, tax filings and similar business functions are motivating examples of what
the storage system must support. They are not its core function or its only use. Any place where a
ledger-specific choice would become a storage-system contract is a defect in this document.

### 1.3 Out of scope

Report's retirement sequence (J4 owns it); fan-out topology (J6); per-unit parallelism; design-time resource
lifetime for stateful sources ([source-model DM12](2026-08-20_data-source-model.md#7-decisions-register));
arbitrary user-authored grammar DSLs; structured writers / lossless
format round-tripping; multi-user hosting and a server-class database; and the UI of any particular domain
application. The structural half is reader-first and assumes the DS execution arc as landed.

### 1.4 Development-phase rule — no compatibility layer

There is no released legacy contract to preserve. Existing code is evidence and useful machinery, not a
compatibility obligation. The design chooses the clean target and replaces obsolete surfaces atomically;
temporary adapters inside one compile-green change are an implementation technique, not a product contract,
and are removed in the same change. `DataShape.Tabular` / `Payload`, `CommonPluginCoordinate` format fields
and duplicate SPI schema trees are not retained merely so old internal notation continues to parse — fixtures
and notation change with the model.

Current behaviour still binds where it demonstrates a real requirement: duplicate input names, buffered
value lifetime, multi-file schema drift and zero-copy scalar access are data / runtime concerns, not legacy
concerns.

## 2. Report is the prototype, not the target

Read as a prototype it is a success, and three of its ideas carry forward unchanged:

1. **Expressions compiled to bytecode and cached.** `CalculatedColumnEval` generates a Kotlin class per
   (expression, columns, model type, parameter types) and caches it through `CachedKotlinCompiler`. This is
   the single most valuable asset in the tree for everything below — §8.
2. **A parse pipeline SPI with a byte-framing stage.** `ReportDefiner` / `ReportDataDefinition` /
   `DataFramer` / `ReportSegmentDefinition`, driven by `ReportInputChain` over a ring buffer.
3. **Format resolution by extension with a priority fallback.** `ReportDefinitionRepository.find` prefers a
   definer claiming the extension, else the highest-priority non-`priorityAvoid` one. `FileDataOpener`
   already delegates to it rather than re-implementing it.

Four things it baked in are now the constraint:

1. **Flat-only records.** `FlatFileRecord` is the universal element and `HeaderListing` the universal
   schema. Nesting has no representation.
2. **Stringly columns.** `HeaderListing` carries names, no types; `ColumnValue` re-derives meaning at
   expression time by coercion.
3. **Format as a bare name.** `PluginCoordinate(val name: String)` — §4.3.
4. **`dataType` as a raw class name in an end-user field.** Report's Input exposes
   `InputSelectionSpec.dataType` (defaulting to `tech.kzen.auto.plugin.model.record.FlatFileRecord`) in the
   UI — a nominal type leaking into a surface where no user can supply a meaningful value.

The DS arc already replaced the fifth limitation (one monolithic document rather than composable Workers).
This document is about 1–3; 4 dissolves once shapes are structural (§5).

## 3. The shape already in the tree

| Concern | Where it lives today | Verdict |
|---|---|---|
| Structural shape | `DataShape.Tabular(HeaderListing)` | Names only, flat only — **generalize** (§5) |
| Nominal shape | `DataShape.Payload(TypeMetadata)` | The nominal descriptor is useful; `Payload` is the wrong shape category. The native type becomes an annotation on the structural node ([unified data model §4.1](2026-08-27_data-model.md#41-the-native-annotation); §5) |
| Declared schema | `DataSchema` document, `DataSchemaFieldListSpec` / `DataSchemaFieldSpec` (field → `TypeMetadata`) | Right vocabulary; the types are parsed and then discarded (§4.1) |
| Element carrier | `JobMessage(payload, flat: FlatView?)` | Two authoritative halves with lossy bridges — **replace with one value over optimized backings** (§11) |
| Flat record | `FlatFileRecord` (`char[]` + `int[]` + lazy caches) | Allocation-free scalar-read / reuse asset; becomes the depth-1 backing (§9.4) |
| Coercing value | `ColumnValue` | Keep as the `Scalar(Text)` accessor; declared non-text types bypass it (§8.5) |
| Expression compiler | `CalculatedColumnEval`, `CachedKotlinCompiler`, `RecordHeaderIndex` | Keep; drive accessor generation from the type instead of the lane (§8) |
| Parse SPI | `ReportDefiner<Output>` / `ReportDataDefinition` / `DataFramer` / `HeaderExtractor<Output>` | Keep the framing pipeline; the `HeaderExtractor` pairing is the ceiling (§4.2) |
| Format identity | `PluginCoordinate` (SPI) / `CommonPluginCoordinate` (common) | A name with no configuration — **make formats values** (§7) |
| Byte provider | `FlatDataSource` / `FlatDataStream` / `FileFlatDataSource` | Useful sequential seam, but insufficient for provider context and range/seek/rows readers (§10) |
| Ref → opener | `DataOpenerLookup` | Provider-bound refs throw; needs provider resolution plus content capabilities, not just dispatch (§10.2) |
| Shape resolution | `DataSourceActions action=shape` → `staticShape` then `inspectShape` | Right order; replace the cases inside `DataShape` with a provenance-bearing observation (§6.2) |
| Long-lived services | `KzenAutoContext` builds `GraphEnvironment.builder()` in its constructor; `kzenAutoInit` installs the `context.close()` shutdown hook | Correct owner for a process-owned store; **no contribution hook exists** (§12.6) |
| Server-owned files | `ManagedStorageRegistry` + `ManagedStorageArea`; `WorkUtils` roots | Right catalogue for store files and attachments; not a permission to evict authoritative records (§12.12) |
| Stable object identity | `ObjectStableMapper` (kzen-lib) | Process-scoped; **not** a persisted reference (§12.9) |

## 4. The finding: information is destroyed at three seams

Not opinions — three places where the tree already computes the right thing and then throws it away. Each
is small, and each is a symptom of the same missing abstraction.

### 4.1 A declared schema's types are discarded at the only place they could matter

`DataSchemaDocument.shape()` is two lines:

```kotlin
fun shape(): DataShape.Tabular =
    DataShape.Tabular(HeaderListing.ofUnique(fields.fields.keys.toList()))
```

`DataSchemaFieldSpec` parses `TypeMetadata` per field — `class` / `of` / `nullable`, recursive, round-trip
pinned by `DataSchemaFieldSpecTest`. The field *names* survive into the shape; every declared type is
dropped, because `HeaderListing` is a list of names and there is nowhere else to put them. The document is
the supply and `staticShape` is the consumer, and the wire between them is one field wide.

### 4.2 Every format's shape is forced through a flat header

`ReportDefiner<Output>` is generic and `ReportDataDefinition` carries `outputModelType: Class<Output>` — the
`kzen-sample-plugin` emits its own `WcpRow`. So arbitrary structure is *expressible* today. But
`ReportDefinition` pairs every definer with a `HeaderExtractor<Output>`, and `FileDataOpener.inspect` is
declared to return `DataShape.Tabular` and constructs one unconditionally:

```kotlin
private fun <T> inspect(spec: EffectiveOpenSpec, definition: ReportDefinition<T>): DataShape.Tabular {
    val headerDefinition = FlatDataHeaderDefinition(...)
    return DataShape.Tabular(ReportHeaderReader().extract(headerDefinition))
}
```

So the only escape from flat is to go nominal, and the only way to go nominal is to compile a class.
**That is why configurable structural format instances in the UI are blocked — it is not merely a UI
gap.** Structural richness has no representation that is not a JVM type, so authoring structure means
authoring code.

### 4.3 A format can be selected but never configured

The whole SPI type is:

```kotlin
data class PluginCoordinate(val name: String)
```

A coordinate carries no parameters, so "semicolon-separated with a fixed header" cannot be *said*, only
*implemented*. The tell is already in the tree: `CsvReportDefiner` and `TsvReportDefiner` are the same class
twice — same `DataEncodingSpec`, same `FlatFileRecord` output, same `FirstRecordItemHeaderExtractor`, same
`FlatPipelineHandoff(true)`, same 32K ring buffer — differing only in which lexer and parser the segment
names. Two identical definers is the shape duplication takes when the varying part has nowhere to live.

## 5. The model — one recursive type language

The type language, its algebra, the shape envelope and the runtime value are owned by the
[unified data model](2026-08-27_data-model.md) — its §4 (types), §4.7 (algebra), §5 (`DataShape`) and §6
(`DataValue` / `ValueAccess`). This section records what this arc contributed to that model and where the
model deliberately departed from the analysis first written here, so the `ST*` register below reads correctly.

Carried into the unified model unchanged:

- `Record`, `Mapping` and `Listing` are different contracts; collapsing them costs either static accessors or
  declared keys (unified §4.3).
- The scalar vocabulary is semantic, not JSON's — integer, floating and decimal distinct, temporal kinds
  distinct, binary distinct from text (unified §4.4).
- `Union` is tagged and comes only from a declared or carried schema; inference that observes incompatible
  variants widens the position to `Dynamic`; nullability is a uniform modifier, never a variant (unified §4.5,
  **ST2**).
- `DataType` is the semantic projection used for access, not a lossless schema IR; a format retains native
  schema metadata beside it when exact re-emission matters (**ST3**).
- `DataShape` is the observation envelope — provenance, stability, diagnostics — around one `itemType`, and
  inspection `Unavailable` stays distinct from an observed `Dynamic` (unified §5, **ST4**; source-model DM9).
- `accepts` / `join` / `project` and `KType → DataType` are part of the contract, with representative-pair and
  wire / digest tests (unified §4.7, **ST14**).

Deliberate departures, decided in the unified model:

- **`Nominal` is `Opaque`, and the native class is a facet of the `DataContract` at every position.** A data
  class is a `Record` whose contract names its class, not a reflected `Nominal`; generated Kotlin reads nested
  native classes from the contract, while type identity and digests stay purely structural. The "reflect a `Nominal` into a `Record`" projection of the original §5.2 becomes the
  data-class / Java-record baseline plus registered adapters; graph-object structural exposure and its member
  policy (**ST5**) are deferred to a concrete processor (unified §4.1, §7.2, §8).
- **No `Enum` scalar kind.** A closed symbol set is `Scalar(Text)` plus schema metadata checked by value
  validation; it reopens with the first carried-schema format that needs enum identity (unified §4.4).
- **Field identity is `FieldId(name, occurrence)`**, the `HeaderLabel` shape; aliases and numeric IDs are
  provider metadata beside the type (unified §4.2).
- **`Dynamic` is an observation / requirement type; every present runtime node is concrete.** The original
  §5.6's "base case with a working accessor" is realized by dispatching on the concrete runtime type, which is
  what the `value["field"]` surface of **ST21** already does (unified §4.6).
- **Decode failure is not a runtime value state** in the first cut; the reader's decode policy (**ST17**)
  settles it before a lane sees the value (unified §4.3).
- **The owner is `kzen-lib-common`**, and `kzen-auto-plugin` depends on kzen-lib for the value contract
  (**ST8**, settled — §15).

One consequence stated in the original §5.7 remains this document's: Report's `dataType` field (§2, constraint
4) dissolves, because "which record model" stops being a user-facing question once native and structural facets
are one type. Format selection uses independent representation hints first and validates the resulting shape
with the type algebra; it cannot select solely by a type that the format may itself be needed to discover (§7.3).

## 6. Parsing arbitrary structure

### 6.1 Unify at emitted values, not byte parsing

A structural event stream — object/list start and end, field identity, typed scalar — is a useful producer
contract for building a shared structural backing. Record-item boundaries are separate events: a nested
object is not automatically one cursor element. Delimited rows and Avro object containers can use a
sequential adapter; JSON / XML selectors must understand parser state; Parquet reads footer metadata and
selected column chunks through range access (its file format is metadata-first and column-oriented —
parquet.apache.org, *File Format*); protobuf wire messages carry no schema and are not self-delimiting, so
they need a declaration and explicit message framing (protobuf.dev, *Techniques*); a relational query
yields typed rows and needs neither bytes nor ranges (§10.3).

The existing `DataFramer` + `ReportSegmentDefinition` pipeline therefore stays as the optimized
**sequential framed-reader adapter**, not as a mandatory foundation for every reader. Readers may expose
sequential bytes, seek / range-readable content, or a native row/object cursor. They unify at `DataValue` /
`ValueAccess` (§8), and an event-to-tape adapter is one implementation of that output contract.

### 6.2 Schema provenance is a resolution ladder, not a flag

Five rungs, tried in priority order — the ladder is the resolution algorithm; the resulting `DataShape` records
which rung won as a single provenance value, and disagreement from a lower rung is a diagnostic:

1. **Declared** — a `DataSchema` document referenced by the source, a store-owned named query with an authored
   result `DataType`, or another explicit contract. No IO for the document case; the only rung the
   definition-time walk may read ([source-model DM6](2026-08-20_data-source-model.md#7-decisions-register)).
2. **Carried** — Avro and Parquet embed a schema; XML may have an XSD. (Protobuf does *not*: a wire message
   needs a `.proto` or descriptor set, which makes it a *declared* schema in a different notation.) The
   format knows and can simply be asked. This is the rung that does not exist today.
3. **Provider-reported** — a provider or driver reports result metadata, but does not supply the semantic
   contract. JDBC `ResultSetMetaData` is the forcing case: SQLite columns have affinities rather than rigid
   value types, and query expressions need not have an affinity, so metadata alone cannot honestly be called a
   declaration (§13).
4. **Inferred** — sample N records and generalize. CSV's header row is the trivial instance; JSON needs
   real sampling, with the sample size a knob and the result explicitly provisional.
5. **`Dynamic`** — nothing is known; the lane is `Dynamic` and each value is read by its concrete runtime
   type (§5, ST21).

`DataSourceActions action=shape` already implements rung 1 and a narrow form of rung 4
(`ReportHeaderReader` infers only names from a header). Widening `inspectShape` from "read a header row" to
"ask the reader what it knows" is the extension, and it is where carried and provider-reported metadata land.
The returned `DataShape` retains provenance, sampling
coverage, provisional status and diagnostics ([unified data model §5](2026-08-27_data-model.md#5-datashape-observes-a-type-it-does-not-classify-a-carrier)). A declaration is the contract; observed disagreement
is a validation diagnostic, never a silent override — **ST6**.

### 6.3 Record selection is composed with the format

Arbitrary structured data is usually one document containing many records at some path: `$.data.items[*]`,
`/root/row`, an Avro container's blocks, "each line" for delimited text. Without a **record-selection path**
on the format, a nested document is one enormous record and every downstream Worker sees a single element.

This sits *inside* a `DataPart` — finer-grained than the unit/part split, which is about which files belong
together — but it is not intrinsically part of a JSON / XML codec or of the dataset schema. Codec / dialect,
record selection and schema / decoding policy have different reuse and identity and should compose beneath
the effective configured format (§7.1). A shared selector abstraction is worthwhile only for the subset it
can stream honestly; codecs may contribute a dialect when their structure requires it — **ST7**.

### 6.4 What the plugin SPI keeps

`ReportDefiner` stays the code-authored sequential extension point and keeps its no-arg-constructor /
`META-INF/kzen/plugins.yaml` discovery and `PluginDocument` registration. Its framing pipeline is a
capability, not the reader SPI itself. Reader capabilities declare the content access they need and emit
`DataValue`; shape inspection can return a carried / inferred observation independently. The
`HeaderExtractor<Output>` pairing becomes the flat special case of shape observation rather than a required
pairing for every output.

### 6.5 Inference and decoding are policies, not reader accidents

Sampling needs record, byte, time, depth and field-count budgets; deterministic field ordering; numeric
promotion / overflow rules; handling for empty and null-only collections; optional-field inference; and the
ST2 rule that incompatible inferred heterogeneity widens to `Dynamic`. Its cache key includes the effective
format, selector, declared schema and inference configuration, and its `DataShape` reports coverage and
provisional status.

Declared typing introduces data-quality failures that text-only input did not distinguish. Before typed
decoding ships, choose whether a malformed value fails the part, skips the record, substitutes null /
default, or enters a quarantine lane. Diagnostics carry source, unit, record and structural path.

Tree readers also require defensive budgets: maximum nesting, record and collection size; XML external
entity disablement; YAML alias-expansion limits; protobuf recursion limits; and bounds for selectors and
regular expressions. These are shipping gates for the first structural reader, not later hardening —
**ST11**, **ST17**, **ST20**.

## 7. Formats are values, not names

### 7.1 The effective format is a composition

A configured format becomes an ordinary notation object assembled from three concerns:

- **Codec / dialect** — for example `DelimitedTextCodec { delimiter, quote, escape, header, trim }`, or a
  contributed JSON / XML / Avro capability. `DelimitedTextCodec` collapses `CsvReportDefiner` and
  `TsvReportDefiner` and buys `;`, `|`, headerless input and non-standard quoting. Fixed-width remains a
  separate physical layout with offsets, widths, padding, alignment and overflow.
- **Record selector** — rows, lines, blocks, or a bounded codec-specific path (§6.3).
- **Schema / decoding policy** — a `DataSchema` reference plus null tokens, temporal patterns, locale,
  overflow and malformed-value policy.

A JSON codec is reused across many record paths and schemas; a dataset schema is usually source-specific,
not an intrinsic property of JSON — which is why the three compose rather than fuse. Code-contributed codecs
are references / capabilities rather than members of a closed `json | xml | yaml` enum; adding one must not
require editing a generic central `when` (CC-17). A `PluginFormat` wraps code-authored JAR definers, while
CSV / TSV coordinates are removed atomically rather than preserved as a parallel product contract (**ST9**).

`DataPart.format` holds an effective configured-format identity rather than only a
`CommonPluginCoordinate`. Because graph objects are mutable, the manifest must either snapshot the immutable
effective configuration or carry the reference plus its resolved definition digest / version — today
`DataPart.digest()` hashes the coordinate string, so a reference whose target changed would open differently
under an identical digest. The opener uses the captured value throughout the run, and manifest / migration /
schema-cache digests include codec, selector, schema and inference configuration — **ST16**.

### 7.2 UI-authored and code-authored formats are the same kind of node

This is the property worth protecting. A configured format authored in the UI is notation; a codec authored
in Kotlin is a contributed capability; both resolve through the same graph and appear in the same catalogue.
Nothing downstream of resolution needs to know which kind supplied the codec. This enables configurable
instances of supported families; it deliberately does **not** claim that the UI can define an arbitrary EDI,
log or binary grammar without a separately designed, bounded parser DSL.

### 7.3 What this does to the format catalogue

The `fileFormats` action starts returning graph-discovered configured formats alongside installed codec
capabilities. Selection separates pre-parse hints from post-parse validation: an explicit format wins; in the
current file-only world extension and priority choose the default, deterministically when several configured
instances claim one extension; future providers may add a trustworthy media-type hint or content / magic
detection. Type compatibility is checked *after* selection because the reader may be needed to discover the
type.

Representation, format and type are three axes, related many-to-many:

```
bytes + media type / content coding
    -> configured codec / dialect / record selector
    -> DataValue + structural DataType
    -> optional nominal JVM projection
```

One structural type can be represented as JSON, XML, Avro or Parquet; `application/json` can carry
innumerable structural types; a domain subtype such as `application/vnd.example.order+json` adds a
representation identity and a syntax hint but still no field schema. A format capability advertises the
media types it accepts; media type helps *select* a format and never *is* the configuration.

Representation vocabulary stays honest without building speculative precedence machinery: rename
`DataPart.encoding` to **character encoding**; name content coding explicitly (`innerExtension()` already
recognizes gzip in embryonic form); reserve a media-type hint in `DataRef.attributes` for a future provider —
nothing in the tree supplies one today. Arbitrary delimiter and selector knobs do not become private MIME
parameters — **ST19**.

Picking a format may mean creating one, which brings naming, shared-versus-inline ownership, deletion, stale
references and recursive schema UX. The UI change is not small; the select is the small part.

## 8. Expression access — generated from the type

### 8.1 The runtime contract comes first

`DataType` describes a semantic value; generated Kotlin needs a runtime value to read. That contract —
`DataValue(access, root)`, `ValueAccess`, the absent / null / present states, the active-variant and selected
node operations, primitive reads without `Any?` boxing, and value lifetime — is the
[unified data model §6](2026-08-27_data-model.md#6-runtime-values-and-multiple-backings). What this section
relies on: the effective lane `DataType` is stable enough to compile against, runtime state tells the accessor
which optional fields and variants are present, and Formula mutation produces one output value and one output
type by owned in-place append on an appendable backing (unified §11.4) rather than updating parallel payload and
flat representations.

### 8.2 What `CalculatedColumnEval` already unifies

It is worth being precise about how much is already right. The generated class binds three scopes: the model
as implicit receiver with a `payload` alias, one accessor per column, one accessor per declared parameter.
Nominal members and structural columns are *already* in one scope, with Kotlin's innermost-receiver rule
resolving collisions. The compile is cached on the shape tuple, and the user's expression is generated as a
lambda-valued probe so one compile serves validation, type inference and execution.

The only thing wrong with it is that every column is generated identically:

```kotlin
private fun columnValue(columnIndex: Int): ColumnValue {
    val index = indices[columnIndex]
    val text = if (index == -1) { DataShape.missingCellValue } else { record.getString(index) }
    return ColumnValue.ofText(text)
}
```

Text, always, regardless of what is known.

### 8.3 Accessors driven by `DataType`

Generate per node kind instead:

| `DataType` | Generated accessor |
|---|---|
| `Scalar(Int / Decimal / Temporal / …)` | typed property off a typed slot — `val amount: Int` |
| `Scalar(Text)` | `ColumnValue` — coercing text, so `amount > 5` on an undeclared column keeps meaning what it means today (ST1) |
| `Record` | a nested accessor over a node index, so `order.customer.city` type-checks |
| `Listing` | typed lazy list façade over child ordinals; materialize a Kotlin `List<T>` only on request |
| `Mapping` | key-lookup accessor, dynamic by construction |
| `Union` | discriminated accessor over `activeVariant` / `selected`, exposing the active tagged case (**ST2**) |
| contract with a native facet | the implicit receiver — the original object — alongside its structural accessors; an `Opaque` node has only the receiver |
| `Dynamic` lane | explicit lookup — `value["field"]` — dispatching on each value's concrete runtime type, followed by typed / coercing conversions; Kotlin cannot resolve an unknown `.field` (**ST21**) |

`payload` and columns stop being two mechanisms. The "fallback for accessing arbitrary structured data" the
user asked for is not a mode — it is the `Dynamic` row of this table. It is also narrower than it looks: an
*inferred* schema (§6.2 rung 3) yields a `Record`, so every field the sample saw gets a generated `.field`
accessor, and only what the sample did not see is `Dynamic`. That is the actual answer to "ergonomic when
the schema is known, a fallback when it is not", and it makes ST11's sampling budget a UX knob rather than a
hazard.

### 8.4 Path resolution is compile-time

`RecordHeaderIndex(columnNames)` already resolves name → position once and hands the generated class an
`IntArray` (`recordHeaderIndex.indices(headerListing)`), so column access is an int index, not a map lookup.
The generalization is **name-path → ordinal-path**, resolved the same way at compile time. Consequently
`order.customer.city` compiles to *constant child ordinals*, not nested lookups.

This is the point worth repeating in any argument about performance: **a declared schema turns nested name
resolution into constant ordinal navigation.** Only `Dynamic` and `Mapping` pay a lookup, which is where the
cost belongs.

### 8.5 `ColumnValue` is the `Scalar(Text)` accessor

`ColumnValue`'s coercion semantics are product behaviour, not a compatibility artefact — comparing a text
column to a number (`"13.0" == 13`) is the bread-and-butter ETL expression — and its interned constants
(`empty`, `null`, `0`, `1`, `true`/`false`, `y`/`n`) are a real allocation optimization. It stays, as the
accessor generated for `Scalar(Text)`, which is what it already is today. What changes is that a *declared*
non-text type no longer routes through it, and `Dynamic` gets an explicit lookup rather than a coercing
node. An undeclared CSV column is `Scalar(Text)` (**ST1**, settled) — the type is honest and no existing
expression changes meaning.

## 9. Performance — preserve fast paths with measured boundaries

### 9.1 State the boundary honestly

`FlatFileRecord` stores `char[] fieldContents` + `int[] fieldEnds`: a field is addressed as
*(buffer, start, end)*, never as an object. Around that sit the three things that do the real work:

- **Lazily populated parallel caches** — `doublesCache`, `hashesCache` — with **caller-owned scratch**
  (`long[] i128` is passed *in* to `cachedDoubleOrNan` / `cachedHash`, not allocated per call).
- **A full reuse protocol** — `clear`, `clearCache`, `copy`, `clone`, `exchange`, `prototype`, `growTo`,
  `growBy` — which is what lets the 32K ring and `FlatPipelineHandoff` recycle instances.
- **Allocation as opt-in** — `getString(int)` is the *only* allocating reader, and `fieldContentsUnsafe()` /
  `fieldEndsUnsafe()` / `contentStart` / `contentEnd` sit beside it for callers that do not want it.

This proves zero-allocation scalar access and reuse **inside Report's ring-buffer pipeline**, not a
zero-allocation Job lane. `FileDataCursor.fill()` prototypes every emitted row, and `DataReadCore.message()`
on the superset path builds a second record via `FlatFileRecord.of(...)` with one `String` per cell. The
first performance target is therefore measured improvement against that baseline. The design must name
which invariant a test covers: parser-slot reuse, allocation-free scalar reads, or end-to-end per-record Job
execution. No benchmark or allocation harness exists in kzen-auto today; the first session that claims an
allocation property builds a narrow pin for it.

### 9.2 Structural tape is one backing, not the universal representation

For nested textual data, one contiguous byte / character buffer plus primitive node tables is a strong
candidate: kind, offsets, child links and interned field ids. But Avro and Parquet expose typed primitives
that should not round-trip through characters, a relational row arrives already typed, and nominal readers
may naturally yield objects. A structural tape therefore may carry typed primitive buffers alongside spans,
while native typed and object backings remain valid implementations of the same `ValueAccess` contract.

The common claim is **one access interface with several backings**, not one character tape or one parser
pipeline. Event streams produce structure; tapes store one representation; `FlatFileRecord` supplies another.
Where a declared schema fixes field identities, generated access uses constant ordinals regardless of the
backing. `Dynamic` and `Mapping` alone pay name / key lookup.

### 9.3 API shape and lifetime determine the allocation boundary

If the record interface exposes `getScalar(i): Any?`, every downstream read boxes and the door is shut
permanently — no later optimization can reopen it. The `ValueAccess` surface that mirrors what `FlatFileRecord`
already proves out — inline `DataNode` handles, primitive reads with caller-owned scratch, opt-in allocating
text — and the lifetime rules are specified by the
[unified data model §6](2026-08-27_data-model.md#6-runtime-values-and-multiple-backings). Two consequences
belong to this arc's readers:

- Generated accessor return types must stay **concrete and monomorphic**: a nullable or generic `DataNode`
  boxes, so a generated accessor never returns `DataNode?` or a `T`. `cachedDoubleOrNan` cannot implement
  exact integers or decimal precision; decimal access may allocate a `BigDecimal`, and binary / temporal
  decoding have their own cache needs — each is declared, not hidden.
- Reuse beyond the parser requires an explicit lifetime. The unified model's first cut makes every value
  self-contained or strongly held and readable while reachable, which is what `FileDataCursor` already does by
  prototyping each row; a release / pool protocol that lets backing arrays return to a ring after queueing,
  batching, sorting or retention — and the retention states and transfer protocol it needs — is introduced by
  the first source that measurably cannot meet the reachability rule — **ST15**.

### 9.4 `FlatFileRecord` is the depth-1 backing, not a casualty

Do not replace it. It is a tuned depth-1 span-backed record on the path that matters most today. Have it
implement `ValueAccess` directly — `kzen-auto-plugin` depends on kzen-lib for the value contract (ST8), so no
forwarding adapter sits on the hottest read path; preserve its primitive caches and ring-buffer protocol; and
measure against the current workload. The structural tape appears only where the type is genuinely nested.
Compatibility of the interface does not itself prove absence of regression.

### 9.5 Typed flat is an optimization opportunity

Today's hot loop is `record.getString(index)` → `ColumnValue.ofText(text)`: two allocations per column read,
softened only by `ColumnValue`'s interned constants. Under a declared schema, a numeric comparison can compile
straight to a primitive cached read with caller scratch. That is a credible improvement hypothesis against
what ships; a benchmark and allocation profile must establish its actual boundary.

### 9.6 Where allocation is unavoidable, declare it

Protobuf into generated messages, Avro `GenericRecord`, an XML DOM, a JDBC `ResultSet` row — these hand you
objects, and that is fine: their backings allocate. The framework must not force boxing or text conversion
on a backing that can avoid it; each reader / backing should declare its capability and lifetime rather than
pretending every format shares one allocation profile.

### 9.7 Pin it with a test

DS3 pinned "every cursor call inside `blocking`" with an offload counter in its A/B. Same discipline here:
benchmark the current Job-lane baseline, then pin allocation-free primitive / span reads and any stronger
parser-slot or end-to-end target separately. A single vague allocations-per-record assertion would conceal
which layer regressed.

## 10. Providers — selection is already separate from processing

### 10.1 The seam exists and is correct

`DataSource.resolve → DataManifest` (which things) versus `DataOpener.open(part) → DataCursor` (what to do
with them) is exactly the separation the user asked for, and DS built it. `FlatDataSource` /
`FlatDataStream` is a useful sequential local-file seam below that, with `FileFlatDataSource` as its only
implementation; it is not yet the provider-neutral content contract.

### 10.2 Two couplings to break

`DataOpenerLookup` refuses provider-bound refs outright:

```kotlin
fun openerFor(ref: DataRef): DataOpener {
    val source = ref.source ?: return plainOpener
    throw IllegalStateException("provider-bound refs are not supported yet: ${source.value}")
}
```

and `FileDataOpener` hardcodes `FileFlatDataSource.instance` rather than resolving a provider for the ref.
Removing those throws is necessary but insufficient: `FlatDataSource` receives only a `FlatDataLocation`,
not the full provider-bound `DataRef`, execution context / client, credentials or resource lifetime.

### 10.3 What a provider-bound source actually costs

A provider-bound source needs a content-access capability approximately "open this resolved `DataRef` as
content." It resolves `DataSourceId` to the provider instance that owns credentials, clients and connections,
retains cancellation / close ownership, and advertises which access it offers — **sequential** bytes,
**range / seek** access, or **rows** (a typed row cursor, for a provider whose data never was bytes). Parquet
over S3 is the forcing case for range: selection remains independent from interpretation, but the reader
needs footer and column range reads supplied by the provider. A relational query (§13) is the forcing case
for rows: it needs neither sequential bytes nor ranges, only typed records, and forcing it through a byte
stream would be a regression in both speed and type fidelity.

That capability, a `DataSource` resolving bucket / prefix or query parameters, and `DataSourceId` minting
([source-model DM11](2026-08-20_data-source-model.md#7-decisions-register)) land with the first provider-bound
source rather than speculatively. Disk / S3 / HTTP / database can
then share the format, schema and expression stack above content access. `DataRef.attributes` already
carries cheap object-store metadata such as size / modified and has the reserved media-type-hint landing
site from §7.3 — **ST18**.

## 11. Worker type flow — one authoritative value

The replacement of `JobMessage(payload, flat)` and `WorkerLane(payloadType, flatColumns)` by one `DataValue`
per element and one lane `DataType` is specified by the
[unified data model §11.4](2026-08-27_data-model.md#114-job), including the three Job behaviours it preserves
by construction — `ColumnValue` coercion for undeclared columns (ST1), `<missing>` as a projection policy under
superset normalization, and owned in-place append for calculated columns. What remains this arc's concern:

- Each Worker declares a type transformation: filters preserve input; Formula appends record fields or
  replaces the root using `KType → DataType`; pivot / sort / summary / writers require structural
  capabilities; a Logic boundary projects into its declared result type; a `Dynamic` lane weakens validation
  without preventing execution.
- Existing column Workers — `CsvWriterWorker`, `ExportWriterWorker`, `PivotWorker`, `SortWorker`,
  `SummaryWorker`, `FilterWorker`, `ValueSetFilterWorker`, `PreviewWorker`, `ExploreWorker` — consume a
  flatten `project`, not an implicit `flatView()`. Its nested naming, listing and mapping rules are part of
  the semantic algebra. This is why a tree reader cannot precede the Worker and projection contract even if
  its parser is independently ready.

## 12. Durable project storage — the generic capability

### 12.1 Why records are not notation

Notation is design-time configuration. The properties that make it good at that are the ones that make it
wrong for records:

- **Whole graph in memory, re-digested per edit.** `DirectGraphStore` caches the `GraphDefinitionAttempt` by
  the notation's content digest; every `NotationCommand` yields a new `GraphNotation` and, on next
  observation, a new define. Fifty thousand rows would make every UI edit re-digest fifty thousand objects.
- **Document-granular persistence.** `YamlNotationParser.unparseDocument` re-emits the changed object's text
  segment inside its document; a document is the write unit. A record store's write unit is a row.
- **No query.** `GraphNotation.firstAttribute` and inheritance-chain lookups are the access model. There is
  no aggregate, no range scan, no join — any total would be a full traversal in Kotlin.
- **A hand-rolled YAML dialect.** `util/yaml/YamlParser.kt` is not a general-purpose serializer; its quoting
  and escaping rules are tuned for authored configuration, not for bulk data.

None of this is a defect. It is the same reason kzen already keeps report runs and Job output *outside*
notation under `WorkUtils` roots. Records are one more thing that is neither notation nor a scratch file.

### 12.2 Where data lives — the tier boundary

| Tier | Contents | Why it belongs there |
|---|---|---|
| **Notation** (`main/` documents, CQRS) | Configuration a user authors and reviews: reference tables with a handful of rows, rules and their effective dates, entity configuration, report templates, `DataSchema` documents, the automation Jobs and Scripts themselves | Low volume, benefits from undo, diffability, rename-as-refactor, and the Notation → Definition → Instance model |
| **Project store** (§12.4) | Records: high volume, transactional, queried by aggregate, referentially constrained, retained for years | Transactions, queries, integrity, retention |
| **Managed storage** (`ManagedStorageRegistry`) | The store files themselves (registered, non-deletable); binary attachments as a budgeted or authoritative area; export bundles | Disk stays centrally accountable — the § 6 convention in kzen-auto `docs/architecture.md` |
| **Run / trace** (in-memory engine projection) | A run's progress and diagnostics | Already how every Logic works; nothing new |

The boundary is semantic, not presentational. **Notation holds authored executable / configuration
definitions where graph identity, inheritance, refactoring and diffability matter. A project store holds
authoritative data requiring transactions, querying, referential integrity, independent retention or effective
dating.** A table-shaped UI does not make something store data, and a document-shaped UI does not make it
notation. Small reference data may live in either tier according to those semantics; volume is evidence, not
the rule.

### 12.3 Named stores and a registry

The central shape question is whether every project has one shared database or a registry of independently
owned stores. A single shared database is initially simple, but unrelated extensions then share one schema
namespace and one migration-version sequence; installing, removing or upgrading one extension becomes
coupled to every other extension using the file; and generated schema tools such as SQLDelight are hard to
compose across independently built modules.

The generic shape is therefore a **`ProjectDataStoreRegistry` of named stores**. A contribution owns the
schema and migrations of its store. A simple application uses one default store and still produces one
database file; a larger project isolates independently versioned facilities. The registry is the stable
framework contract; one-file-versus-several is a deployment and ownership choice, not a global invariant.
The central service is named for the capability — `ProjectDataStore` / `ProjectDatabase` — never for a
domain; a domain module injects its own typed repository, which borrows transactions from the store.

A **named store is an atomic transaction boundary**. No framework contract promises an atomic transaction
across stores; data that must commit or roll back together belongs in one store even when several features use
it. Store ownership follows transactional cohesion, not merely package or plugin ownership. This rule also
keeps a future engine substitution honest: the registry cannot assume SQLite-specific cross-file attachment
semantics.

Each registered store needs: a durable store identity and project-relative location; engine / driver
creation; schema creation and migrations it owns; transaction execution and connection-concurrency policy;
managed-storage registration; backup / snapshot participation; initialization and shutdown; and optional
exposure through the kzen `DataSource` model (§13) — **PS2**.

Named stores also make project-level coordination explicit. Startup preflights every contribution before
mutating any store, takes the project recovery snapshot required by PS8, and reports a partially migrated
project as a recoverable boot failure rather than continuing with mixed schema versions. Project backup
quiesces project writes — store transactions, notation commands and authoritative attachment changes — then
snapshots each store, notation and attachments and writes a manifest of store identities, schema versions and
checksums. Separate `VACUUM INTO` calls without that barrier are individually consistent but are not one
project-wide point-in-time snapshot.

### 12.4 One project, one process, one root

Each kzen-auto JVM process owns exactly one `KzenAutoContext` and knows nothing of multiple projects; the
project layer is kzen-launcher / kzen-shell. A project process does not receive the launcher's
`--project.home`; kzen-shell's `MainJarProcess` starts it with the project's own directory as the working
directory, and `KzenAutoContext` already treats `../../logs` as cwd-relative. Stores live at an explicit location
such as `<projectRoot>/data/<store-id>/`, outside the notation resource tree; the physical notation path differs
between development and packaged layouts. A project is the application / lifecycle / distribution boundary,
not a claim that it contains only one dataset. The host exposes an explicit **project-root abstraction** rather
than asking contributions to infer it from `WorkUtils` or incidental process layout — **PS1**.

### 12.5 Engine

The server is JVM-only (Ktor); the JS frontend never touches the store, so the choice is purely a JVM
question, and the distribution constraint (§12.13) is "ships inside `main.jar` + `dependencies/` and runs on
the OS / architecture combinations the packaged application tests and supports".

| Engine | Verdict |
|---|---|
| **SQLite via `org.xerial:sqlite-jdbc`** — **recommended default** | Single database per store; native libraries for Windows / Linux / macOS on x64 and aarch64 travel inside the jar, extracted at runtime; no install; WAL mode; single-writer matches the single-user, one-process-per-project profile. Users and auditors can open the database with off-the-shelf tools. A single store can be snapshotted consistently via `VACUUM INTO`; §12.3 coordinates a project-wide backup |
| H2 (embedded) | Pure Java — the fallback if native extraction ever bites on an exotic platform. Weaker external tooling; a history of on-disk format changes across major versions, which matters for records that must open years later |
| DuckDB | Columnar / analytical. Wrong for row-level transactional writes, but a strong *second* engine later for reporting directly over CSV / Parquet exports. Larger natives. Not for the first cut |
| PostgreSQL (embedded-postgres or external) | Multi-user hosting only. Spawns a platform-specific server process; contradicts the one-process-per-project model (§12.4) |

Column representation is the **store owner's** decision, guided not mandated. Values whose semantics require
exactness must not silently use binary floating point: a store may choose a scaled integer, canonical decimal
text or another lossless representation and must declare the scale / precision semantics it exposes. Calendar
dates, local date-times and instants remain distinct; no generic rule converts one into another. These map onto
`ScalarKind`'s decimal and temporal kinds ([unified data model §4.4](2026-08-27_data-model.md#44-scalar-kinds-are-semantic-not-json-derived)) when rows are read through the generic source. The guidance is
recorded because retrofitting either is a data migration — **PS3**.

### 12.6 The seam that does not exist: host lifecycle contribution

`KzenAutoContext` builds `graphEnvironment` as a fixed `GraphEnvironment.builder()` chain inside its own
constructor, and `kzenAutoInit(args, jsModuleName, buildInfo)` takes no service contributions. A downstream
app (the kzen-project pattern — `KzenProjectMain` delegates to `kzenAutoInit` + `kzenAutoMain`) therefore
has **no way to register a `@Service` type today**, and `ServiceEnvironmentValidation.validate` would fail the
boot the moment a project-owned `@Reflect` step declared `@Service private val store: SomeRepository`.

A `(GraphEnvironment.Builder) -> Unit` callback plus an unrelated close hook is too narrow a fix. Contributions
are supplied to `KzenAutoContext.create` **before** construction so the host can complete one ordered lifecycle:

1. resolve the explicit project data root (§12.4);
2. collect and preflight store descriptors, acquire the project / migration lock and establish the recovery
   point required by PS8;
3. create the store(s), run migrations and contribute initialized graph-facing services to the builder before
   `graphEnvironment.build()`;
4. register managed-storage areas;
5. expose initialized services to server routes, detached actions and runs; and
6. close contributions in reverse order at shutdown (`kzenAutoInit`'s existing shutdown hook calls
   `context.close()`).

The existing construction-cycle rule holds — nothing may resolve the environment during `KzenAutoContext`
construction — so contributed services that need context members register as memoized providers, the way
`logicTrace` and `serverLogicController` do. This is the first kzen-auto change any storage consumer needs,
and it is useful to every downstream app — **PS4**.

### 12.7 Ownership: the process owns the store, graph objects borrow

Every relevant rule already points the same way: `GraphInstanceCache`'s reuse contract is "stateless
objects" (extension-points § 3); a data source **borrows, never owns** a resource
([source model §3.3](2026-08-20_data-source-model.md#33-context-capabilities-and-resource-ownership); D4);
and `KzenAutoContext` is the composition root where contributed long-lived services belong. Today its
`close()` closes only `ServerLogicController`; PS4 extends it to close contributions in reverse order.

So the store (for SQLite: a single writer connection plus a small reader pool, or one serialized
connection — measure) is a **process-owned service** reached through `@Service` constructor parameters. What
is registered is the store and the domain's typed repository; raw `DataSource` / `Connection` never appears
in a step's constructor, and graph-instantiated objects never receive or hold a long-lived connection.

A process-owned project store is application infrastructure and **stays process-owned**. It does not later
become a `DesignSession` resource: `DesignSession` (extension-points § 3) addresses resources owned by
*graph-configured providers* — an external JDBC or authenticated API connection used during detached
design-time requests. A generic source over the internal store borrows per-operation connections from the
process-owned service; design-time access (browsing a table, previewing an import) runs through detached
actions and `GraphInstanceCache` statelessly on the same borrow-not-own seam — **PS5**.

JDBC is blocking, so the store contract cannot leave execution semantics implicit. A transaction runs as a
synchronous callback on a counted / dedicated blocking dispatcher; the callback must not suspend while it
holds a connection or database lock. Cancellation and shutdown stop new admission, settle or cancel admitted
work according to an explicit policy, then close connections. The project / migration lock acquired during
PS4 prevents a second process pointed at the same project from racing startup or schema migration.

### 12.8 Write models, and the notation command log

A general store must support immutable / append-only records, mutable CRUD state, effective-dated records,
derived projections and binary-document references. It imposes none of them globally; the write model is the
store owner's. (A double-entry ledger is naturally append-only with reversing corrections and period-close
markers — §14 — and that is a property of ledgers, not of the store.)

The one place *not* to route business writes: `NotationCommand`. Business writes are store transactions with
their own atomicity; the notation event log is for configuration. Do not invent a domain `*Command` that
mirrors the notation reducer — the two histories have different owners, granularities and consumers —
**PS6**.

### 12.9 Persisted references across tiers

A row that names a notation object — a record → its category in a configuration document, an import → the
`DataSchema` it used — must not store an `ObjectLocation` (rename-mutable) and must not store an
`ObjectStableId` either. `ObjectStableMapper` preserves an identity across renames only within one process
and recreates identities from current locations after restart (kzen-lib `docs/architecture.md` § Stable
identity: "assumes a single linear history of notation changes"). Persisted cross-tier references need a
**minted UUID in notation**, duplicate-ID validation and an ID-to-current-location resolver — the same
pattern already selected for provider-bound `DataSourceId`
([source-model DM11](2026-08-20_data-source-model.md#7-decisions-register)). The editor / command machinery may be
shared, while semantic ID types and duplicate scopes remain capability-specific — **PS7**.

Identity alone does not define historical meaning. Every persisted reference declares whether it is **live**
(resolve the referenced object's current definition) or **captured** (retain the definition digest / version or
an immutable snapshot used when the record was written). ST16 makes the same distinction for effective format
configuration. Consumers that must reproduce a historical result cannot rely on a stable UUID pointing at
mutable current notation.

### 12.10 Access layer and migrations

The framework contract is **transaction- and lifecycle-oriented**; the access layer is per store. A store
owner may implement its typed repository with SQLDelight, Exposed, jOOQ or direct JDBC without leaking that
choice into graph objects:

| Option | Fit |
|---|---|
| **SQLDelight** — preferred for a first-party store | Schema-as-source `.sq` files, generated typed Kotlin, `.sqm` migrations, SQLite-first; the closest cultural match to kzen's KSP-generated, no-magic style. Cost: one more Gradle plugin in a build already delicate around KMP and the composite; its JVM driver wraps `sqlite-jdbc` anyway. Its schemas are generated at build time, so it is **not** the public project-storage abstraction — independently contributed schemas do not form one shared history |
| Exposed | Lighter touch, DSL + DAO, SQLite dialect supported; less compile-time checking of the SQL itself |
| jOOQ | Best SQL typing and reporting-query ergonomics; codegen from a live schema; free for SQLite / H2 |
| Plain JDBC + hand-written row mapping | Adequate for a spike; do not let it become the product |

Migrations are a first-class deliverable regardless of layer — records outlive versions. Schema version has
**one authority**: SQLDelight's SQLite driver already uses `PRAGMA user_version`, so a separate
`schema_version` table exists only if it records different application-format metadata. Every store needs
migration verification from checked-in older-schema fixtures, refusal to open a schema newer than the running
code (the launcher's per-project process model means an old build can be pointed at a new project folder),
and a defined pre-migration recovery path. Migrations run at context init before the environment is
validated — **PS8**.

### 12.11 Routes and the UI boundary

`ktorMain(context)` is a public `Application` extension with a fixed `routing {}` block; `kzenAutoMain`
wraps it in `embeddedServer`. A downstream app can bypass `kzenAutoMain` and compose
`embeddedServer { ktorMain(context); routing { /* own routes */ } }` itself — workable without a kzen-auto
change, at the cost of duplicating `kzenAutoMain`'s small body; a route-contribution hook belongs in the
same lifecycle as PS4 but is not blocking. The reverse-proxy contract (`/<process-name>/<subpath>`,
kzen-shell) is unaffected as long as routes are served relative to the process root — **PS9**.

The UI is the larger gap, and it is the one extension-points § 2 already scoped. A separately built
application following the `kzen-project` pattern owns its Kotlin/JS bundle and registers its own generated JS
module before delegating to the kzen-auto client, so it can contribute a dedicated `DocumentController` in
its own `-js` module **today**. The generic-editor → server-described-form → Web Component ladder remains
important for *runtime-loaded plugins* that cannot rebuild the frontend; it is not a prerequisite for a
compiled downstream application. A domain UI may still be substantial product work, but that cost is
separate from the generic storage seam — **PS10**.

Two adjacent, already-known extension constraints: bundled notation must live under `auto-common/`,
`auto-jvm/` or `auto-js/` (the `AutoConventions.serverAllowed` / `clientUiAllowed` fixed sets), and object
names are graph-global, so a downstream app prefixes its own.

### 12.12 Operational contracts

The general storage system makes the following explicit regardless of its first consumer:

- **SQLite connection policy** — foreign keys on every connection, busy timeout, journal and synchronous
  settings, checkpoint policy and write serialization.
- **Local-filesystem requirement** — WAL-backed live databases are not safe on arbitrary network or
  cloud-synchronized filesystems; say so, and detect what can be detected.
- **A consistent snapshot / backup / restore contract** — copying a live database file or casually zipping a
  project is not that contract, particularly in WAL mode. A single-store snapshot may use `VACUUM INTO` or
  the online backup API; a project backup first quiesces writes across every store, then captures all stores,
  notation and authoritative attachments with one manifest of store identities, schema versions and checksums.
- **Import idempotency and duplicate detection** where consumers ingest external records.
- **Binary attachments** — checksums, filesystem / DB failure recovery, orphan handling and retention policy.
- **Privacy, filesystem permissions, encryption requirements** and the intended tamper-evidence threat model.
- **A tested OS / architecture support matrix** for the native driver and packaged runtime.
- **Native-library extraction policy** verified by the packaging spike; any override uses an application cache /
  work area, never durable project data.
- **The complete jlink module set**, derived and package-tested, rather than a single `java.sql` assumption.

Managed storage provides visibility and lifecycle controls; it is **not** permission to budget-evict durable
records or evidence. Each store or attachment contribution declares which contents are derived / deletable
and which are authoritative / non-deletable. For SQLite, the main database and any live `-wal` / `-shm`
sidecars are one logical managed bundle: a generic `DirectoryStorageArea` must not expose them to cleanup or
deletion as independent children — **PS11**.

### 12.13 Cross-platform distribution

What exists: `:kzen-shell:distWindows` (jars + bundled Temurin JDK + `.bat` launchers, the turnkey Windows
zip) and `:kzen-shell:distJars` (jars only, any OS with a JDK ≥ toolchain). What is planned
([`../plans/2026-07-25_desktop-and-hosting.md`](../plans/2026-07-25_desktop-and-hosting.md)): a
jpackage Windows installer with a jlink-trimmed runtime (DA3), Linux deb / rpm (DA4), macOS parked until a
mac user appears (DA6); jpackage does not cross-compile, so each platform builds on its own host. A domain
app rides that pipeline as a project archetype zip (`kzen-<app>-<v>.zip`, the `main.jar` + `dependencies/`
contract) pulled by the launcher — the same path kzen-project already takes.

SQLite changes none of that surface. Three operational notes:

- **Native extraction directory.** `sqlite-jdbc` extracts its native library to `java.io.tmpdir` unless
  `org.sqlite.tmpdir` is set. Do not override this speculatively: the packaging spike must test extraction,
  cleanup, writable-directory constraints, signing and endpoint-security behaviour on each supported target.
  Set `org.sqlite.tmpdir` to a managed application cache / work area only if that test finds a concrete need
  or the packaging layout requires it; never point it at durable project data. `org.sqlite.lib.path` /
  `org.sqlite.lib.name` remain an alternative if packaging deliberately pre-places the native library.
- **jlink module set.** When DA3 trims the runtime, `java.sql` — and whatever else the driver and access
  layer load — must be in the module list, derived from the packaged application and tested, because a
  trimmed image that omits a module fails at first connection, not at build time.
- **Per-platform natives are already in the jar.** No platform-specific dependency declaration; the Linux
  and mac bundles pick up the right library at runtime. H2 would remove even that, at the cost in §12.5 —
  **PS12**.

## 13. The generic database source — where the two halves meet

A `RelationalQuerySource` / `DatabaseQuerySource` over a registered store is a legitimate first
provider-bound `DataSource`, and it is the concrete consumer that justifies structural-data Half 1 (typed
flat). It implements `DataSource` (resolve: a registered-store query with bind parameters → one `DataUnit`)
and, as the [source model §3.2](2026-08-20_data-source-model.md#32-resolution-and-opening-stay-separate)
anticipates, the opener on the same object, because a query result cannot be read
without its provider. Its content capability is `rows` (§10.3).

The primary surface is a **store-owned named query** with explicit parameter types and, where the store can
author it, an explicit result `DataType`; that result is `Declared`. JDBC result metadata is instead
`ProviderReported`: SQLite is dynamically typed, and an expression column may have no stable affinity, so
metadata alone cannot honestly promote a result to a declared semantic contract. An optional raw-SQL surface
is an explicit advanced capability, not the default notation contract.

Execution safety is part of the provider contract, not left to each caller: resolve an authorized named
store; default to read-only; accept values only through bound parameters; allow one query statement; enforce
row, byte and elapsed-time budgets; support cancellation; and keep the result set, statement and borrowed
connection owned by the row cursor until that cursor closes. A named query may deliberately expose a
domain-restricted view (posted entries, for example), but that is application notation over the generic
surface, never a framework type.

It is not a small adapter. It requires durable `DataSourceId` minting and resolution
([source-model DM11](2026-08-20_data-source-model.md#7-decisions-register) — using shared
editor / command machinery with PS7, not necessarily the same semantic ID type), provider lookup in
`DataOpenerLookup`, and the typed-row `ValueAccess` backing with a cursor-lifetime contract. Because it
crosses several framework seams, it **follows rather than gates** the first vertical use of project
storage: a repository-backed detached action or domain Worker validates persistence and transactions before
the generic source lands. Writes go the other direction through ordinary Workers / steps that call a
repository; nothing in Job needs to know it is writing to a database — **PS13**.

## 14. The motivating example — a ledger

Recorded as the acceptance test that shaped the requirements above, and as the place ledger-specific
decisions live so they do not leak into §12:

- **What it demands of the store:** append-only posted entries with reversing corrections and period-close
  markers; exact decimal money with a currency exponent; fiscal dates; years of retention; auditability ("what
  did the books say on filing date" as a query, not a backup restore); binary attachments (receipts, PDFs)
  with checksums; import idempotency for bank feeds; filings whose figures are reproducible.
- **What stays the example's own:** money representation and rounding policy; chart-of-account placement;
  filings and their reproducibility rules; whether two legal entities share a project or use separate
  projects (the framework supports either, and multiple named stores / datasets within one project);
  ledger, invoice and reconciliation UI strategy; the notation / store split of its particular
  configuration (chart of accounts, tax rules and rates with effective dates, entity and fiscal-year
  configuration, report templates, import `DataSchema`s in notation; journal, postings, documents, parties,
  inventory movements and cost layers, reconciliation state, filings in the store).
- **What it proves for the framework:** PS4 (a downstream app registering a `@Service` repository), PS5
  (borrow-not-own through detached actions), PS8 (migrations across versions), PS11 (backup / restore of a
  WAL database), and §13 (typed rows read by existing Job / Report cards before any bespoke UI exists — a
  visible generic-source payoff).

## 15. Naming and packages

- `DataType`, `DataShape`, `DataValue` / `ValueAccess` and the binding vocabulary live in `kzen-lib-common`
  ([unified data model §14.1](2026-08-27_data-model.md#141-ownership)); `HeaderListing` stays in
  kzen-auto-common as the tabular projection's header until its consumers move to structural access. **ST8 is
  settled** by dropping the "dependency-free plugin" constraint rather than working around it: `kzen-auto-plugin`
  (JVM-only today) depends on the JVM variant of kzen-lib-common, `FlatFileRecord` implements `ValueAccess`
  directly, and third-party readers emit the value contract with no mirror or boundary adapter. There is no
  external plugin ecosystem or released contract to protect; the sample plugin recompiles.
- Format objects: `kzen-auto-jvm` notation under `auto-jvm/data/`, with the `DataSchema` document that
  already lives in `server/objects/data/schema/`.
- Structural tape and typed backing implementations: `kzen-auto-plugin` `jvmMain`, beside
  `FlatFileRecord`; neither is the public parser pipeline.
- Project storage: `ProjectDataStoreRegistry` / `ProjectDataStore` and the lifecycle contribution in
  `kzen-auto-jvm` `server/context/` beside `KzenAutoContext`; the SQLite engine and its connection policy in a
  `server/service/storage/` sibling of `ManagedStorageRegistry`; the generic query source in
  `server/objects/datasource/` with the file source. Domain schemas, repositories and UI live in the
  consuming application's own modules, never in kzen-auto. Physical stores live under
  `<projectRoot>/data/<store-id>/`, not in the notation resource tree.

## 16. Decisions register

Settled only where stated; **Current proposal** records conclusions adopted by this analysis but still open
to an implementation decision. `ST*` are structural-data decisions; `PS*` are project-storage decisions
(they replace the former `BS*` register — the mapping is noted where a decision was corrected). Neither
collides with the DS document's `O*`.

### 16.1 Structural data

| # | Question | Current thinking |
|---|---|---|
| ST1 | Type and accessor for an undeclared delimited field | **Settled: `Scalar(Text)`**, accessed as coercing `ColumnValue` (§8.5). Declared non-text types bypass it; `Dynamic` means unknown structure |
| ST2 | Tagged and inferred variants | **Adopted by the unified model (its §4.5, UD5):** `Union` comes from declared / carried schema and every runtime node carries its active variant — from an external tag where the encoding has one, otherwise by unique-accepting-variant structural selection with ambiguity failing; inferred incompatible heterogeneity widens to `Dynamic`; nullability is a uniform modifier |
| ST3 | Semantic access projection or lossless schema IR | **Current proposal:** `DataType` is the semantic access projection; native schema metadata may live beside it. Structured writing / lossless round-trip is out of scope (§5) |
| ST4 | Does `DataType` replace `DataShape`? | **Settled 2026-08-27:** replace today's `Tabular` / `Payload` cases, but retain `DataShape` as the provenance / stability / diagnostic observation envelope, with Declared, Carried, ProviderReported, Inferred and RuntimeOnly provenance and Unavailable distinct from Observed (unified §5, §6.2 here; source-model DM9) |
| ST5 | Native → structural projection policy | **Narrowed by the unified model:** data classes and Java records are the automatic structural baseline, registered adapters cover other classes, and no public-getter reflection exists; graph-object exposure metadata and navigation are deferred to a concrete processor (unified §7.2, §8) |
| ST6 | Observation disagrees with declaration | Declaration is the contract; observation produces location-aware validation diagnostics in `DataShape`, never a silent override (§6.2) |
| ST7 | Record-selector dialect | Share only an honestly streaming-safe subset; a codec may contribute its own bounded dialect (§6.3) |
| ST8 | Owner of the recursive type tree | **Settled 2026-08-27:** `kzen-lib-common` owns the vocabulary; `kzen-auto-plugin` depends on kzen-lib and `FlatFileRecord` implements `ValueAccess` directly. The dependency-free-plugin constraint is dropped, not worked around — there is no external contract to protect (§15; unified §14.1, §14.3) |
| ST9 | Configured delimited formats versus CSV / TSV coordinates | **Replace atomically.** Existing `format: CSV` / `TSV` notation breaks intentionally and all fixtures move in the same change (§7.1, §1.4) |
| ST10 | One tape or one access contract | **Adopted by the unified model (its §6, UD3):** one `ValueAccess` contract with flat, structural-tape, native typed / row and object backings. Share a tape where representations agree; do not force readers through it (§9.2) |
| ST11 | Inference budget and result | Record count plus byte / time / depth / field-count budgets; deterministic join rules; coverage and provisional status returned in `DataShape`. Defaults remain to choose |
| ST12 | Field presence and nullability | **Adopted by the unified model (its §4.3):** presence / default belong to the ordered field; nullability applies uniformly to `DataType`; absent, null and defaulted remain distinct. Decode failure is settled by ST17 reader policy rather than carried as a runtime value state in the first cut |
| ST13 | Scalar and variant detail | **Adopted with departures (unified §4.2, §4.4):** parameterized scalar kinds and stable variant tags; no `Enum` kind and no open-ended logical-type registry in v1; exact numerics lower as canonical text inside the existing `ExecutionValue` grammar until a versioned wire grammar is justified (unified §12) |
| ST14 | Type algebra | **Adopted and split (unified §4.7):** `isAssignable` under an explicit structural / native requirement, `validate` for value conformance, `project` for flattening, `join` with associativity / commutativity / idempotence laws, and `KType → DataType`, all with representative-pair and wire / digest tests |
| ST15 | Runtime value lifetime | **Adopted with a v1 rule (unified §6.2, UD23):** every value is self-contained or strongly held and readable while reachable, with no public retention state; the first source that measurably cannot meet that copies before emission or introduces retain / release or a pool protocol, before any end-to-end zero-allocation claim (§9.3) |
| ST16 | Effective-format identity | Snapshot immutable effective configuration or capture reference + resolved digest / version; manifests and caches include codec, selector, schema and inference settings (§7.1) |
| ST17 | Decode-error policy | Decide fail-part, skip-record, null / default substitution and quarantine semantics; diagnostics include source, unit, record and path before typed decoding ships |
| ST18 | Provider content access | Resolve the full `DataRef` through its provider and advertise `sequential`, `range` / seek and `rows` capabilities; land with the first provider-bound source (§10.3, §13) |
| ST19 | Representation metadata | Rename character encoding; model content coding explicitly; reserve provider media type as a hint without building precedence before a supplier exists (§7.3) |
| ST20 | Security and writer scope | Structured writers are excluded from this reader arc. Before a tree reader ships, set nesting / record / collection / selector limits and format-specific protections |
| ST21 | Dynamic expression surface | Explicit keyed / indexed lookup dispatching on each value's concrete runtime type, followed by typed or coercing conversions; unknown `.field` is not generated (§8.3; unified §4.6, UD22) |

### 16.2 Project storage

| # | Question | Current thinking |
|---|---|---|
| PS1 | Where a store lives | **Adopt:** `<projectRoot>/data/<store-id>/`, outside notation resources and resolved through an explicit host project-root abstraction. A project is a lifecycle / application / distribution boundary and may contain multiple independent datasets or named stores (§12.4). *(was BS1)* |
| PS2 | Store shape | **Adopt:** a `ProjectDataStoreRegistry` of named stores, each owning its schema and migrations; one default store for simple apps. A named store is an atomic transaction boundary; data requiring one transaction belongs together, while project migration and backup coordinate all stores without claiming cross-store atomic commits (§12.3) |
| PS3 | Engine and column representation | **Adopt SQLite (`sqlite-jdbc`)** as the default engine; H2 the recorded pure-Java fallback; DuckDB deferred to a reporting phase. Semantically exact values use a declared lossless representation such as scaled integer or canonical decimal text; dates, local date-times and instants remain distinct — store-owner guidance, not a framework rule (§12.5). *(was BS3, BS4)* |
| PS4 | Host lifecycle contribution | **Required, not built.** Contributions are supplied to `KzenAutoContext.create` before construction. One ordered lifecycle acquires the project lock, preflights descriptors and recovery, initializes / migrates stores and services before `graphEnvironment.build()`, then registers managed areas, routes / actions / runs and closes in reverse order; it respects the no-resolve-during-construction rule (§12.6, §12.7). *(was BS7)* |
| PS5 | Ownership | **Adopt:** process-owned store service via `@Service`; graph objects stateless, borrow per operation; the internal store never becomes a `DesignSession` resource — that seam is for graph-configured external providers (§12.7). *(corrects BS5)* |
| PS6 | Write models | **Adopt:** the store supports immutable, CRUD, effective-dated, projection and binary-reference models and imposes none; business writes never go through `NotationCommand` (§12.8). *(generalizes BS6; append-only is the ledger's choice — §14)* |
| PS7 | Persisted cross-tier references | **Adopt:** minted UUID in notation with duplicate-ID validation and an ID → location resolver; editor / command machinery may be shared with `DataSourceId`, while semantic ID types and duplicate scopes stay capability-specific. Every persisted reference declares live-current versus captured-historical semantics; never use `ObjectStableId` (§12.9). *(corrects BS2)* |
| PS8 | Access layer and migrations | Transaction / lifecycle contract is the public seam; SQLDelight preferred per first-party store, Exposed if its Gradle plugin proves costly in the composite; one schema-version authority (`PRAGMA user_version` under SQLDelight); older-fixture migration tests, refuse-newer guard, project-wide preflight / recovery snapshot and recoverable partial-failure handling (§12.3, §12.10). *(was BS10)* |
| PS9 | REST route contribution | Bypass `kzenAutoMain` and compose `ktorMain` + own routes; a hook in the PS4 lifecycle is nicer but not blocking (§12.11). *(was BS8)* |
| PS10 | UI boundary | A compiled downstream app contributes its own `DocumentController` today; the generic-editor / form / Web Component ladder serves runtime-loaded plugins; domain UI cost is separate from the storage seam (§12.11). *(was BS9, narrowed)* |
| PS11 | Operational contracts | Connection policy, local-filesystem requirement, coordinated project backup / restore with manifest, import idempotency, attachment integrity, privacy / encryption / tamper model, support matrix, native extraction policy and derived jlink module set. Managed storage never evicts authoritative records and treats a SQLite database plus live WAL / SHM sidecars as one bundle (§12.12) |
| PS12 | Distribution | Keep the driver's default native extraction unless the packaging spike finds a concrete reason to set `org.sqlite.tmpdir` to a managed cache / work area; derive and test the jlink module set when DA3 trims the runtime; natives ride in the jar (§12.13). *(was BS12)* |
| PS13 | Generic database source | `RelationalQuerySource` as a provider-bound `DataSource` with `rows`: store-owned named queries are primary and may declare their result; JDBC metadata is ProviderReported; raw SQL is an explicit constrained capability with binding, read-only default, budgets, cancellation and cursor-owned resource lifetime. It follows the first vertical storage consumer rather than gating it (§13). *(was BS11)* |

## 17. Build order — proposed

Two arcs, separately decidable, with one meeting point (§13). Structural data has two halves: typed flat
closes the immediate information-loss seam and validates generated access over `FlatFileRecord`; structural
readers can be scheduled later on their own merit. Project storage proves persistence and transactions
through one vertical consumer before the generic source joins the two.

### Structural data — Half 1, typed flat

Steps 0–4 of the original order are now the [unified data model's sequencing](2026-08-27_data-model.md#145-sequencing),
whose steps 1–5 land the type and algebra, the shape envelope (`DataSchemaDocument.shape()` stops discarding
types), the value over flat / native / fake typed-row backings, the Job carrier, and bindings. ST8 is settled
(§15), so there is no separate ownership-gate step; the module preparation it implied — `kzen-auto-plugin`
depending on kzen-lib and `FlatFileRecord` implementing `ValueAccess` — is part of the value step. What this
arc adds beside those steps:

4. **Expressions.** Generate typed, `ColumnValue` and dynamic accessors in `CalculatedColumnEval` from the
   lane `DataType` (§8), with name-path → ordinal-path resolution at compile time (§8.4), once the Job carrier
   carries one value.

### Structural data — beside either half

5. **Configured-format proof.** Compose delimited codec / dialect, record selection and text-decoding
   policy; replace CSV / TSV coordinates and fixtures atomically; capture immutable effective-format
   identity in manifests and cache keys; apply the representation decisions in ST19.
6. **Provider-bound content.** Add provider resolution and the `sequential` / `range` / `rows` content
   capabilities with the first provider-bound source; mint `DataSourceId` here
   ([source-model DM11](2026-08-20_data-source-model.md#7-decisions-register), shared with PS7).
   The first provider-bound source may be the generic database source (S5) or an object-store source,
   whichever a consumer demands first.

### Structural data — Half 2, structural readers

7. **Structural backing + first reader.** JSON through a bounded streaming selector, covering declared,
   inferred and dynamic shapes. Use the flatten `project` from step 1 for existing column Workers; impose
   ST11, ST17 and ST20 policies before shipping it.
8. **Validate the reader / content SPI.** Avro exercises sequential carried schema; Parquet exercises
   metadata-first columnar and range access. Do not freeze the SPI before both.
9. **Structured writers only under a separate scope decision.** If introduced, specify ordering, variant
   discriminators and native-schema preservation rather than assuming the reader projection round-trips.

### Project storage

- S1a. **Host lifecycle seam.** Add the pre-construction contribution API, project-root abstraction,
  project / migration lock, ordered initialization before graph build and reverse-order shutdown. This is
  independently useful without committing to one database engine.
- S1b. **Storage and recovery contract.** Add the store registry, named-store transaction boundaries,
  coordinated migration preflight / failure recovery and project backup / restore manifest. Keep this
  separate from S1a: its multi-store consistency contract is not a small lifecycle edit.
- S2. **Spike** SQLite and the preferred access layer inside the actual composite build before adopting the
  access-layer choice (PS8).
- S3. **Storage end to end.** Schema creation, migrations (older-fixture upgrade, refuse-newer), restart
  persistence, transaction invariants, blocking-dispatcher / shutdown rules, managed registration of the
  database + WAL + SHM bundle and backup / restore tests; add a multi-store test for coordinated backup and
  partial-migration recovery. A headless smoke boots the server on a spare port, writes through a repository
  and reads an aggregate back.
- S4. **One thin consumer.** A domain repository plus a downstream document controller or detached action —
  the ledger or another domain as the acceptance test, contributing no framework vocabulary.
- S5. **The generic database source** (§13, PS13), after S3 proves the storage semantics and after structural
  steps 2 and 3 provide typed `Record` shapes and their runtime `ValueAccess` backing. It also fulfills
  structural step 6's provider-resolution / `rows` capability if nothing else has claimed that slot.
- Later, only on demand: DuckDB reporting over exports; multi-user hosting.

## 18. As-built

*(Sessions append here as each lands — deviations, surprises, and anything a later reader could not
recover from the code.)*
